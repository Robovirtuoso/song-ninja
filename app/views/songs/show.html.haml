%h3= best_in_place [:user, @album.band, @album, @song], :title, type: :input

%blockquote
  %p= best_in_place [:user, @album.band, @album, @song], :lyrics, type: :textarea, display_with:  -> v { v.gsub(/[\r\n]+/, '<br>').html_safe }
